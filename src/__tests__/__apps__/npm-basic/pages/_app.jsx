import 'shared-ui/styles/global.css';
import 'shared-ui/styles/another-global.scss';
import 'shared-ui/styles/yet-another-global.sass';

export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
